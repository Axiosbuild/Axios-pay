Continue building the Axios Pay platform. add backend files missing.

Still needed in backend/src/routes/:
- kyc.ts: POST /kyc/documents (multipart upload, call interswitchService for BVN/NIN/Passport/DriversLicense verification based on documentType, store result, update kycTier), GET /kyc/status, POST /kyc/facial-compare
- webhooks.ts: POST /webhooks/paystack (verify HMAC x-paystack-signature header, credit wallet on success, idempotent), POST /webhooks/flutterwave (verify x-flw-signature)
- health.ts: GET /health returns { status: "ok", db: "ok", redis: "ok", uptime }
- admin.ts: GET /admin/users (paginated), GET /admin/kyc/queue, POST /admin/kyc/:id/approve, POST /admin/kyc/:id/reject, GET /admin/transactions

Still needed in backend/src/services/:
- email.service.ts: nodemailer with sendVerificationEmail(), sendPasswordResetEmail(), sendTransactionReceipt() using HTML templates
- socket.ts: setupSocketHandlers(io) — authenticate JWT on handshake, join user to room user:{userId}, emit rate updates every 60s

Still needed: backend/src/lib/socket.ts

For the frontend (apps/web — Next.js 14 App Router):

Design system: Dark pan-African luxury fintech. Colors: --green-900: #0A2318, --green-400: #2D9E6B, --gold-400: #F0A832. Fonts: Clash Display (headlines), Cabinet Grotesk (body), JetBrains Mono (all amounts). NO generic AI aesthetics.

Build these pages with full API integration:
1. / — Landing page: animated SVG African continent, live rate ticker, how it works, trust bar
2. /auth/register — 4-step flow with Framer Motion transitions
3. /auth/login — with 2FA support
4. /dashboard — balance card, wallet grid, live rates (WebSocket), recent transactions
5. /dashboard/exchange — 3-step swap: configure → 30s countdown quote → success + confetti
6. /dashboard/history — paginated, grouped by date, slide-out detail drawer
7. /dashboard/kyc — tier ladder, drag-drop upload, progress tracking
8. /dashboard/settings — profile + 2FA management

State: Zustand stores for auth, wallets, fx rates. TanStack Query for server state. Axios with auto-refresh interceptor. Socket.IO WebSocket client with exponential backoff reconnect.

Infrastructure:
- docker-compose.yml: postgres:15, redis:7, api (build: ./backend), web (build: ./frontend)
- Dockerfile for backend (multi-stage, node:20-alpine)
- Dockerfile for frontend (Next.js standalone output)
- .github/workflows/deploy.yml: build images → push to DO Container Registry → kubectl rolling deploy → health check → rollback on failure
- infra/terraform/main.tf: VPC, DOKS cluster, managed Postgres, managed Redis, Spaces bucket, Load Balancer, DNS for axiospay.africa
- infra/k8s/: api-deployment.yaml (HPA 2-10 pods), ingress with TLS cert-manager
